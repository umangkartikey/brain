# FORGE — Biologically-Inspired Cognitive Architecture
# Core dependencies
# Install with: pip install -r requirements.txt

# HTTP API (each module runs as an independent Flask server)
flask>=3.0.0

# Terminal UI (rich display for all module demos)
rich>=13.0.0

# Standard library only beyond this point —
# no external ML frameworks required.
# FORGE is intentionally dependency-light.

# Optional: for forge_mind_v2 orchestrator HTTP calls
requests>=2.31.0

# Optional: for forge_swarm parallel workers
# (uses threading from stdlib, requests for inter-module calls)

# Development / testing only
# pytest>=7.0.0
